{
    "db-mysql": {
        "prefix": "db-mysql",
        "body": [
            "DB_CONNECTION=${1:mysql}",
            "DB_USERNAME=${2:root}",
            "DB_PASSWORD=${3:Password}",
            "DB_HOST=${4:localhost}",
            "DB_PORT=${5:3306}",
            "DB_DATABASE=${6:colegio}",
            "",
            "DB_UI_DEV=\"${DB_CONNECTION}://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_DATABASE}\""
        ],
        "description": "mysql template for dadbod ui"
    }
}
